# Required metadata
sonar.projectKey=python:PySkCode
sonar.projectName=PySkCode
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=skcode

# Language
sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# File exclusions
sonar.exclusions=**/tests/**,**/tests.py
